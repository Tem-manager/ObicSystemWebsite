import React from "react";

type Props = {
  // أضف هنا خصائص (props) التي تحتاجها الدالة
};

const ObicAi: React.FC<Props> = (props) => {
  return (
    <div>
      أضف المحتوى الذي تريده هنا
    </div>
  );
};

export default ObicAi;
